const jáfiz = true;

const checaExerciciosDaPrimeiraParte = (jáfiz) => {
  if (jáfiz) {
    console.log('Recebi uma iluminação divina e fiz todos os primeiros exercícios com funções. Obrigado senhor.');
  } else {
    console.log('Hora de refatorar tudo. F.');
  }
};

checaExerciciosDaPrimeiraParte(jáfiz);